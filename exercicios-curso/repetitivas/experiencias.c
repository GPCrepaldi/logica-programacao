#include <stdio.h>

int main () {

    int testes, cobaia, total, tc, tr, ts;
    double pc, pr, ps;
    char tipo;

    printf("Quantos casos de teste serao digitados? ");
    scanf("&d", &testes);

    for (i = 0; i <= testes; i++) {
        printf("Quantidades de cobaia: ");
        scanf("%d", &cobaia);

        printf("Tipo de cobaia: ");
        scanf("%c", &tipo);

        if (tipo == 'c' || tipo == 'C') {
            
        }
        
    }
    
}